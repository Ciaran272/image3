├── index.html                              # 生产环境入口HTML
├── favicon.png                             # 图标
├── manifest.json                           # PWA配置文件
├── vite.svg                                # Vite默认Logo
│
├── package.json                            # 项目依赖和脚本（dependencies + devDependencies）
├── package-lock.json                       # 依赖版本锁定文件
│
├── vite.config.ts                          # Vite构建配置（base路径、打包优化、CORS头设置）
├── tsconfig.json                           # TypeScript编译配置
├── tsconfig.node.json                      # TypeScript编译配置
│
├── 📁 assets/                              # 构建产物静态资源目录
│   ├── index-B-MauwpB.css                  # 所有样式合并打包
│   ├── index-CSwepiA0.js                   # 主应用代码（React组件+业务逻辑，压缩混淆）
│   └── vendor-Cf0Y5DJ5.js                  # 第三方库打包（React、TensorFlow.js、Upscaler等）
│
└── 📁 src/                                 # 源代码
    │
    ├── main.tsx                            # 应用入口文件（ReactDOM.createRoot + 渲染App组件）
    ├── App.tsx                             # 根组件
    ├── App.css                             # 根组件样式
    ├── index.css                           # 全局样式（CSS变量定义、通用样式、禁止文本选中）
    ├── types.ts                            # 核心类型定义（ImageItem、ProcessOptions、ProcessResult、ProcessingStats）
    │
    ├── 📁 components/                      # UI组件目录
    │   ├── Header.tsx                      # 顶部导航栏组件
    │   ├── Header.css                      # 导航栏样式
    │   │
    │   ├── UploadZone.tsx                  # 拖拽上传区域
    │   ├── UploadZone.css                  # 上传区样式
    │   │
    │   ├── ImageList.tsx                   # 图片列表管理页面
    │   ├── ImageList.css                   # 列表页样式（header布局、按钮排列、响应式设计）
    │   │
    │   ├── ImageGallery.tsx                # 图片缩略图网格组件
    │   ├── ImageGallery.css                # 缩略图样式
    │   │
    │   ├── GlobalSettings.tsx              # 全局设置面板
    │   ├── GlobalSettings.css              # 设置面板样式
    │   │
    │   ├── ProcessingPanel.tsx             # 处理进度面板
    │   ├── ProcessingPanel.css             # 进度面板样式
    │   │
    │   ├── ResultsView.tsx                 # 处理结果页
    │   ├── ResultsView.css                 # 结果页样式
    │   │
    │   ├── StyledSelect.tsx                # 自定义下拉选择器组件（替代原生select，支持紧凑模式）
    │   └── StyledSelect.css                # 下拉选择器样式（紧凑模式、悬停效果、选项列表动画）
    │
    ├── 📁 utils/                           # 工具函数目录
    │   ├── imageProcessor.ts               # 核心图像处理（基础放大、Canvas渲染、DPI写入、流程编排）
    │   ├── aiUpscale.ts                    # AI超分辨率封装（TensorFlow.js初始化、Upscaler.js模型加载、WebGL/CPU切换）
    │   ├── processingQueue.ts              # 任务队列管理（并发控制、错误处理、进度回调、状态更新）
    │   └── globalReset.ts                  # 全局重置逻辑（清除localStorage/sessionStorage、页面刷新）
    │
    └── 📁 types/                           # TypeScript类型声明目录
        ├── potrace.d.ts                    # Potrace库类型声明（位图转矢量化算法接口）
        └── upscaler-models.d.ts            # Upscaler.js模型类型声明（ESRGAN 2x/3x/4x模型接口）

